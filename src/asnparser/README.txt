One day I will find the ASN.1 parser and put it here.

It was based on asnparser from PWLib which is was part of OpenH323.

OpenH323 is now called OPAL and PWLib was renamed to PTLib (although parts of it have always been called PTLib).
